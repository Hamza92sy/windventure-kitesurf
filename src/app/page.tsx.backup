"use client";

import React from "react";
import Link from "next/link";
import { motion } from "framer-motion";
import PremiumHeroFuturiste from "@/components/PremiumHeroFuturiste";
import GalaxyPackageCard from "@/components/GalaxyPackageCard";
import CyberTestimonials from "@/components/CyberTestimonials";
import NeonButton from "@/components/NeonButton";
import ParticleBackground from "@/components/ParticleBackground";

export default function Home() {
  // Variants d'animation pour les sections
  const sectionVariants = {
    hidden: { opacity: 0, y: 50 },
    visible: {
      opacity: 1,
      y: 0,
      transition: {
        duration: 0.6,
        ease: "easeOut" as const,
      },
    },
  };

  const cardVariants = {
    hidden: { opacity: 0, scale: 0.9 },
    visible: {
      opacity: 1,
      scale: 1,
      transition: {
        duration: 0.5,
        ease: "easeOut" as const,
      },
    },
    hover: {
      scale: 1.02,
      y: -5,
      transition: {
        duration: 0.3,
        ease: "easeOut" as const,
      },
    },
  };

  return (
    <main className="min-h-screen bg-gradient-futuristic">
      {/* Background avec particules */}
      <ParticleBackground particleCount={100} interactive={true} />

      {/* Hero Section Futuriste */}
      <PremiumHeroFuturiste />

      {/* Featured Package avec animations */}
      <motion.section
        className="container mx-auto px-4 py-20 relative z-10"
        variants={sectionVariants}
        initial="hidden"
        whileInView="visible"
        viewport={{ once: true, amount: 0.3 }}
      >
        <motion.div
          className="text-center mb-16"
          variants={sectionVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <h2 className="text-4xl md:text-5xl font-bold text-white mb-4">
            Most Popular Package
          </h2>
          <p className="text-xl text-white/80">
            Perfect for those looking for the complete kitesurfing experience
          </p>
        </motion.div>

        <motion.div
          className="max-w-2xl mx-auto"
          variants={cardVariants}
          initial="hidden"
          whileInView="visible"
          whileHover="hover"
          viewport={{ once: true }}
        >
          <GalaxyPackageCard
            pkg={{
              id: "combined",
              title: "Combined Package",
              description: "Our most comprehensive offering, blending technique mastery with adventure exploration.",
              price: 1350,
              image: "/images/package-combined.jpg",
              category: "combined",
              isPopular: true,
              features: [
                "Best of both worlds",
                "12 hours training",
                "3 different spots",
                "Technique & adventure",
                "Complete experience",
              ],
            }}
            categoryColors={{
              beginner: "from-neon-green to-neon-cyan",
              advanced: "from-neon-blue to-neon-purple",
              exploration: "from-neon-pink to-neon-purple",
              combined: "from-neon-purple to-neon-pink",
            }}
            categoryIcons={{
              beginner: "🌊",
              advanced: "⚡",
              exploration: "🗺️",
              combined: "🎯",
            }}
          />
        </motion.div>
      </motion.section>

      {/* Testimonials Cyber */}
      <CyberTestimonials />

      {/* Why Choose Us avec animations */}
      <motion.section
        className="bg-gradient-to-r from-neon-blue/20 to-neon-purple/20 py-20 relative overflow-hidden"
        variants={sectionVariants}
        initial="hidden"
        whileInView="visible"
        viewport={{ once: true, amount: 0.3 }}
      >
        <div className="container mx-auto px-4 relative z-10">
          <motion.div
            className="text-center text-white max-w-4xl mx-auto"
            variants={sectionVariants}
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
          >
            <h2 className="text-4xl md:text-5xl font-bold mb-12">
              Why Choose Windventure?
            </h2>
            <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
              {[
                {
                  icon: "🏄‍♂️",
                  title: "Expert Instructors",
                  description:
                    "Certified professionals with years of experience in Dakhla's unique conditions",
                },
                {
                  icon: "🌊",
                  title: "Perfect Location",
                  description:
                    "Dakhla offers ideal wind conditions and stunning lagoons for all skill levels",
                },
                {
                  icon: "⚡",
                  title: "Premium Equipment",
                  description:
                    "Latest generation kites and boards, perfectly maintained for your safety",
                },
              ].map((item, index) => (
                <motion.div
                  key={index}
                  className="text-center bg-glass-white backdrop-blur-md rounded-2xl p-8 border border-neon-blue/20"
                  variants={cardVariants}
                  initial="hidden"
                  whileInView="visible"
                  whileHover="hover"
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.2 }}
                >
                  <motion.div
                    className="text-5xl mb-6"
                    animate={{ rotate: [0, 5, -5, 0] }}
                    transition={{ duration: 3, repeat: Infinity, ease: "easeInOut" }}
                  >
                    {item.icon}
                  </motion.div>
                  <h3 className="text-2xl font-bold mb-4 text-white">{item.title}</h3>
                  <p className="text-white/80 text-lg leading-relaxed">{item.description}</p>
                </motion.div>
              ))}
            </div>
          </motion.div>
        </div>
      </motion.section>

      {/* CTA Section avec animations */}
      <motion.section
        className="container mx-auto px-4 py-20 relative z-10"
        variants={sectionVariants}
        initial="hidden"
        whileInView="visible"
        viewport={{ once: true, amount: 0.3 }}
      >
        <motion.div
          className="text-center max-w-3xl mx-auto"
          variants={sectionVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
            Ready for Your Adventure?
          </h2>
          <p className="text-xl text-white/80 mb-12 leading-relaxed">
            Join hundreds of satisfied adventurers who have discovered the thrill of
            kitesurfing in Dakhla.
          </p>
          <motion.div
            className="flex flex-wrap justify-center gap-6"
            variants={cardVariants}
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
          >
            <NeonButton href="/packages" variant="glow" size="lg">
              <motion.span
                animate={{ rotate: [0, 10, -10, 0] }}
                transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
              >
                🏄‍♂️
              </motion.span>
              Explore All Packages
            </NeonButton>
          </motion.div>
        </motion.div>
      </motion.section>
    </main>
  );
} 